# Ecco UI — Distribution Payload

> Internal encrypted distribution repository.

This repository contains the encrypted Ecco UI runtime payload loaded by `loader.lua`.

**Do not reference this repository directly.** Use the public loadstring instead:

```lua
local Ecco = loadstring(game:HttpGet("https://raw.githubusercontent.com/eritcgx-cmyk/ecco-ui/main/loader.lua", true))()
```

See the [public repository](https://github.com/eritcgx-cmyk/ecco-ui) for documentation, examples, and API reference.
